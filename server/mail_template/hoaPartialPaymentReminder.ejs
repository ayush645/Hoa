<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Partial Payment Reminder</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
        margin: 0;
        padding: 0;
        background-color: #f4f4f4;
        color: #333;
      }
      .email-container {
        max-width: 600px;
        margin: 20px auto;
        background: #ffffff;
        border-radius: 8px;
        overflow: hidden;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
      }
      .header {
        text-align: center;
        padding: 15px;
      }
      .header .logo {
        max-width: 120px;
        margin-bottom: 10px;
      }
      .header h1,
      .header h2,
      .header h3 {
        margin: 5px 0;
      }
      .email-header {
        text-align: center;
        padding: 15px 20px;
      }
      .email-header h1 {
        margin: 0;
        font-size: 24px;
      }
      .email-body {
        padding: 20px;
      }
      .email-body p {
        margin: 10px 0;
      }
      .email-footer {
        background: #f4f4f4;
        padding: 15px;
        text-align: center;
        font-size: 14px;
        color: #666;
      }
      .stamp {
        margin-top: -100px;
        display: flex;
        justify-content: flex-end; /* Align to the right */

        width: 80%;

        opacity: 1;
      }
      .stamp img {
        height: 120px;
      }
    </style>
  </head>
  <body>
    <div class="email-container">
      <!-- Logo and Property Info -->
      <div class="header">
        <img src="<%= logoPath %>" alt="Logo" class="logo" />
        <h2>Property Info: <%= propertyAddress %></h2>
        <h3>To: <%= ownerName %></h3>
      </div>

      <!-- Main Header -->
      <div class="email-header">
        <h1>Subject: Complete Your Partial HOA Contribution</h1>
      </div>

      <!-- Email Body -->
      <div class="email-body">
        <p>Dear <strong><%= memberName %></strong>,</p>
        <p>
          This is a reminder regarding the incomplete payment of your HOA
          contribution for the month of <strong><%= dueMonth %></strong>, which
          remains partially unpaid following the expiration of
          <strong><%= followingMonth %></strong>.
        </p>
        <p>
          <strong>Outstanding Amount:</strong> $<%= outstandingAmount.toFixed(2)
          %>
        </p>
        <% if (dueDate) { %>
        <p><strong>Due Date for Completion:</strong> <%= dueDate %></p>
        <% } %>
        <p>
          Thank you for your attention to this matter, and we appreciate your
          continued support for our community.
        </p>
        <p>Warm regards,</p>
        <p><%= contactInfo %></p>
      </div>

      <!-- Footer -->
      <div class="stamp">
        <img src="https://i.ibb.co/428Fp08/h1.png" alt="Stamp" />
      </div>
    </div>
  </body>
</html>
